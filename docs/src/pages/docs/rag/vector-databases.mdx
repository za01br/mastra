## Step 3: Vector Storage

Store embeddings in a vector database for efficient querying. Mastra supports PgVector, Pinecone, Qdrant, and more.

**Example with PgVector:**

```typescript showLineNumbers copy
import { PgVector } from '@mastra/rag';

const pgVector = new PgVector("postgresql://localhost:5432/mydb");

await pgVector.upsert("embeddings", embeddings);
```

For details on each vector store’s configuration and usage, see the reference docs for [PgVector](/docs/reference/rag/pgstore.mdx), [Pinecone](/docs/reference/rag/pinecone.mdx), and [Qdrant](/docs/reference/rag/qdrant.mdx).
