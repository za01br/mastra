// @ts-nocheck
export type TComponents = {
  securitySchemes: {
    bearerAuth: {
      type: 'http';
      scheme: 'bearer';
    };
  };
};
export const components = {
  securitySchemes: {
    bearerAuth: {
      type: 'http',
      scheme: 'bearer',
    },
  },
} as TComponents;
