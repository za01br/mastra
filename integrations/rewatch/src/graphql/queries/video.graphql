query Video($id: ID!) {
  node(id: $id) {
    __typename
    ... on Video {
      id
      title
      description
      presentedAt
      duration
      url
      thumbnailUrl
      attendeesInfo
    }
  }
}
